import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    boxEmpty: {
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center'
    },
    title: {
        fontWeight: "700",
        fontSize: 16,
        color: '#808080'
    },
    subtitle: {
        fontSize: 12,
        color: '#808080'
    }
});